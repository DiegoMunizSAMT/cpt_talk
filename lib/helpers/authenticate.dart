import 'package:firebase_auth/firebase_auth.dart';

class Authenticate {
  /// Istanza di Firestore per tutto quello che riguarda le autenticazioni.
  static final FirebaseAuth firebaseAuth = FirebaseAuth.instance;
}
